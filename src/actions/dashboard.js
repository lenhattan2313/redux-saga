import * as dashboardConstant from "../constant/dashboard";

export const showSidebar = () => ({
  type: dashboardConstant.SHOW_SIDEBAR,
});
export const hideSidebar = () => ({
  type: dashboardConstant.HIDE_SIDEBAR,
});
